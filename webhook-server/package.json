{
  "name": "webhook-server",
  "version": "1.0.0",
  "description": "server for listening github webhook event",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "combwl",
  "license": "ISC",
  "dependencies": {
    "github-webhook-handler": "^0.7.1",
    "moment": "^2.24.0"
  }
}
